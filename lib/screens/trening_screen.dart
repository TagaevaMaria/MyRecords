import 'package:flutter/material.dart';

class TreningScreen extends StatelessWidget {
  const TreningScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
